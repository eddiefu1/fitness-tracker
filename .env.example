# Copy this file to `.env` in the project root and fill in values.
# Next.js loads `.env` automatically for local dev.

# FatSecret (optional). If missing—or DISABLE_FATSECRET=1—food search uses Open Food Facts only.
FATSECRET_CLIENT_ID=
FATSECRET_CLIENT_SECRET=
# DISABLE_FATSECRET=1

# Daily digest email (Gmail SMTP + Vercel Cron). Use an App Password: Google Account → Security → 2-Step Verification → App passwords.
# No spaces around = (e.g. GMAIL_USER=user@gmail.com). If login fails with 535, generate a new App Password and paste it again.
# DAILY_EMAIL_ENABLED=false
GMAIL_USER= eddie9595@gmail.com
GMAIL_APP_PASSWORD=  
# Optional; defaults to GMAIL_USER
EMAIL_TO= eddie9595@gmail.com
# Required on Vercel: random string; Vercel Cron sends Authorization: Bearer <CRON_SECRET>
CRON_SECRET=
# Production URL (links in the email)
NEXT_PUBLIC_APP_URL=https://your-app.vercel.app

# After filling the Gmail vars above, send a one-off test: npm run email:test
